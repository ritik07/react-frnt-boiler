import React from 'react'
import { WebHeader } from '../../components/webHeader'

const WebLayouts = () => {
    return (
        <div>
            <WebHeader/>
        </div>
    )
}

export default WebLayouts
